{% extends "./Layouts/Base.twig"%}

 {% block content %}
 <header class="bg-blue-500 text-white text-center py-12">
        <h1 class="text-4xl font-bold">Share Your Knowledge with the World</h1>
        <p class="mt-2 text-lg">Join a community of developers writing about the latest in tech</p>
    </header>

    <main class="max-w-4xl mx-auto py-8 px-4">
        <div class="grid gap-6">
        {% for article in articles %}
            <article class="bg-white p-6 rounded-lg shadow-md">
                <h2 class="text-2xl font-bold">{{article.title}}</h2>
                <p class="text-gray-600 mt-2">{{article.content}}</p>
                <a href="/article?id={{article.id}}" class="text-blue-600 font-semibold mt-4 inline-block">Read More</a>
            </article>
             {% else %}
        <p>No articles found.</p>
    {% endfor %}
        </div>
    </main>
 {% endblock %}
